<!DOCTYPE html>
<html>
  <head>
    <title>WanLife</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Kiwi+Maru&display=swap" rel="stylesheet">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="top-page-header">
      <div class='search-bar-contents'>
        <%= link_to image_tag('logo.png', alt: 'ロゴ画像'), root_path, class: "wan_logo" %>
      </div>
      <div class="nav">
        <div class="search-menu"></div>
        <ul class=" lists-left"> <%# 画面左側に全て横並びでメニュー表示 %>
          <li><button id="filterDogCafe">ドッグカフェ</button></li>
          <li><button id="filterDogRun">ドッグラン</button></li>
          <li><button id="filterBoth">どちらも表示</button></li>
        </ul>
        <ul class='lists-right'>
        <% if user_signed_in? %>
          <li><%= link_to "#{current_user.nickname}さん & #{current_user.dogname}ちゃん", "#", class: "user-nickname" %></li>
          <li><%= link_to 'ログアウト', destroy_user_session_path, data: {turbo_method: :delete}, class: "logout" %></li>
        <% else %>
          <li><%= link_to 'ログイン', new_user_session_path, class: "login" %></li>
          <li><%= link_to '新規登録', new_user_registration_path, class: "sign-up" %></li>
        <% end %>
        </ul>
      </div>
    </header>

    <%= yield %>


  </body>
</html>
